
import React from 'react';
import ReactDOM from 'react-dom/client';
import PlotTwistMoviesApp from './PlotTwistMoviesApp';

const root = ReactDOM.createRoot(document.getElementById('root')!);
root.render(
  <React.StrictMode>
    <PlotTwistMoviesApp />
  </React.StrictMode>
);
